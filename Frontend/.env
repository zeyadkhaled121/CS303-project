# In development, the Vite proxy handles forwarding /api requests to the backend,
# so the base URL is empty (requests go to the same origin).
# In production, set this to your deployed backend URL (e.g. https://api.yourdomain.com).
VITE_API_BASE_URL=
